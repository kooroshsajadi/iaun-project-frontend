import { ServiceDirective } from './service.directive';

describe('ServiceDirective', () => {
  it('should create an instance', () => {
    const directive = new ServiceDirective();
    expect(directive).toBeTruthy();
  });
});
